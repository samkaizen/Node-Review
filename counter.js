
let counter = (arr)=>{

    return (
        ` there are ${arr.length} elements in this array `

    );

}


let result = counter(['Shaun', 'Sam', 'Sandra']);

//console.log(result);

module.exports = counter;